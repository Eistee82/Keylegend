# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.12.0.schema.json

PackageIdentifier: Eistee82.Keylegend
PackageVersion: 1.0.0
MinimumOSVersion: 10.0.19041.0
InstallerType: inno
# Left to winget: it knows the silent switches for Inno and fills them in itself.
Scope: user
InstallModes:
- interactive
- silent
- silentWithProgress
UpgradeBehavior: install
# What the installer writes to HKCU, so winget can tell an installed copy from a new one and
# offer upgrades. Inno appends _is1 to the AppId; both values were read back from a real install
# rather than derived.
AppsAndFeaturesEntries:
- DisplayName: Keylegend
  DisplayVersion: 1.0.0
  Publisher: Keylegend contributors
  ProductCode: '{8B4F2C31-9E7A-4D26-B5C8-1F3A6D0E9B47}_is1'
  UpgradeCode: '{8B4F2C31-9E7A-4D26-B5C8-1F3A6D0E9B47}'
  InstallerType: inno
# The .NET Desktop Runtime, which the installer otherwise only links to. Through winget it can
# simply be fetched first — which is the answer to the one real drawback of shipping
# framework-dependent rather than self-contained.
Dependencies:
  PackageDependencies:
  - PackageIdentifier: Microsoft.DotNet.DesktopRuntime.10
ReleaseDate: 2026-08-22
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/Eistee82/Keylegend/releases/download/v1.0.0/Keylegend-1.0.0-setup.exe
  # Regenerated by tools/winget-manifest.ps1 from the published file. The value below is a
  # placeholder: a manifest whose hash does not match what the release actually serves fails
  # validation, which is exactly what should happen.
  InstallerSha256: 499f4ca1feb19b82a98e575c42f62e740dc4c32929b14e1ea0c9dab0a01c56af
ManifestType: installer
ManifestVersion: 1.12.0
